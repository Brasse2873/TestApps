<Window x:Class="UpdateSourceOnEachKeyPress.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UpdateSourceOnEachKeyPress"
        mc:Ignorable="d"
        Title="MainWindow" Height="87.674" Width="525"
        ResizeMode="NoResize">
    <Grid>
        <TextBox HorizontalAlignment="Left" VerticalAlignment="Top"
                 Text="{Binding Path=Src, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock HorizontalAlignment="Left" VerticalAlignment="Bottom"
                 Text="{Binding Path=Src}"/>
    </Grid>
</Window>
